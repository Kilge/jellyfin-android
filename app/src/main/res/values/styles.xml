<resources>
    <!-- Application theme -->
    <style name="AppTheme.V21" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/jellyfin_primary</item>
        <item name="colorPrimaryDark">@color/jellyfin_primary_dark</item>
        <item name="colorAccent">@color/jellyfin_accent</item>
        <item name="android:windowBackground">@color/theme_background</item>
        <item name="android:navigationBarColor">@color/theme_background</item>
    </style>

    <style name="AppTheme" parent="AppTheme.V21" />

    <style name="AppTheme.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/theme_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <!-- Settings theme -->
    <style name="AppTheme.Settings" />

    <!-- Settings screen title: 17sp and 87% black/white like the web
         skinHeader page title. -->
    <style name="SettingsHeaderTitle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">17sp</item>
        <item name="android:textColor">@color/settings_header_icon</item>
    </style>

    <!-- Theme Overlays -->
    <style name="ThemeOverlay.App.BackButton" parent="">
        <item name="colorControlNormal">?android:textColorPrimary</item>
    </style>

    <!-- Player UI is always black, so force dark overlay (white foregrounds) regardless of theme -->
    <style name="ThemeOverlay.App.Player" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorControlHighlight">#33ffffff</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
